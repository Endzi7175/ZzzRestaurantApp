package com.ktsnwt.restaurant.domain.enumeration;

/**
 * The Order Item Status ENUM,\nRepresents the current status of order item.
 */
public enum OrderItemStatus {
    CREATED,
    ACCEPTED,
    PREPARED,
    SERVED,
    CANCELED,
}
